# 个人常用termux命令
## 更新全部
```shell
cd ~/my/blogRoot/ && git pull origin src && cd ~/blog/blogRoot/ && git pull origin src && cd ~/blog/blog/ && git pull origin master && cd ~/my/blog && git pull origin master && cd ~/blog/JavaReadingNotes/ && git pull origin master && cd ~/my/JavaReadingNotes/ && git pull origin master && cd ~/blog/exam/ && git pull origin master && cd ~/my/exam/ && git pull origin master
```
### 命令
```shell
cd ~/my/blogRoot/
git pull origin src
cd ~/blog/blogRoot/
git pull origin src
cd ~/blog/blog/
git pull origin master
cd ~/my/blog
git pull origin master
cd ~/blog/JavaReadingNotes/
git pull origin master
cd ~/my/JavaReadingNotes/
git pull origin master
cd ~/my/ReadingNotes/
git pull origin master
cd ~/blog/ReadingNotes/
git pull origin master
cd ~/blog/exam/
git pull origin master
cd ~/my/exam/
git pull origin master

```
## 更新主站
```shell
cd ~/my/blogRoot/ && git pull origin src && cd ~/blog/blogRoot/ && git pull origin src
```
## 更新博客
```shell
cd ~/blog/blog/ && git pull origin master && cd ~/my/blog/ && git pull origin master
```
## 更新java读书笔记
```shell
cd ~/blog/JavaReadingNotes/ && git pull origin master && cd ~/my/JavaReadingNotes/ && git pull origin master
```
## 更新读书笔记
```shell
cd ~/my/ReadingNotes/  && git pull origin master  && cd ~/blog/ReadingNotes/  && git pull origin master && 
```
## 更新笔试题
```shell
cd ~/blog/exam/ && git pull origin master && cd ~/my/exam/ && git pull origin master
```